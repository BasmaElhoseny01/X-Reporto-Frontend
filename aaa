</Descriptions>
<FormContainer form={form} layout="vertical" initialValues={patient} onFinish={onFinish}>

    <>

        {/* <Descriptions layout="vertical" colon={false}>
            <Descriptions.Item label="Patient ID">{patient.id}</Descriptions.Item>
            <Descriptions.Item label="Patient Name">
                {edit ? (
                <Form.Item name="patient_name" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required:
                    true, message: "Name is required" }]}>
                    <Input placeholder="Patient Name" defaultValue="Basma" />
                </Form.Item>
                ) : (
                <>{patient.patient_name}</>
                )}
            </Descriptions.Item>
            <Descriptions.Item label="Birth Date">
                {edit ? (
                <Form.Item name="birth_date" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required: true,
                    message: "Birth Date is required" }]}>
                    <Input placeholder="Birth Date" type="date" onChange={handleBirthDateChange} />
                </Form.Item>
                ) : (
                <>{patient.birth_date}</>
                )}
            </Descriptions.Item>
            <Descriptions.Item label="Email">
                {edit ? (
                <Form.Item name="email" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required: true,
                    message: "Email is required" }]}>
                    <Input />
                </Form.Item>
                ) : (
                <> {patient.email}</>
                )}
            </Descriptions.Item>
            <Descriptions.Item label="Phone">
                {edit ? (
                <Form.Item name="phone_number" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required:
                    true, message: "Phone is required" }]}>
                    <Input />
                </Form.Item>
                ) : (
                <>{patient.phone_number}</>
                )}
            </Descriptions.Item>
            <Descriptions.Item label="Age">{patient.age}</Descriptions.Item>
            <Descriptions.Item label="Gender">
                {edit ? (
                <Form.Item name="gender" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required: true,
                    message: "Gender is required" }]}>
                    <Radio.Group buttonStyle="solid">
                        <Radio.Button value="male">Male</Radio.Button>
                        <Radio.Button value="female">Female</Radio.Button>
                    </Radio.Group>
                </Form.Item>
                ) : (
                <>{patient.gender}</>
                )}
            </Descriptions.Item> */}
            {/* <Descriptions.Item label="Created At"> */}
                {/* {formatDateTime(patient.created_at)} */}
                {/* {patient.created_at} */}
                {/* </Descriptions.Item> */}
            {/*
        </Descriptions>
        {edit && (
        <ButtonContainer>
            <PrimaryButton htmlType="submit" size="large" style={{ width: "6%" }}>
                Save
            </PrimaryButton>
            <SecondaryButton onClick={()=> {
                setIsEditing(false);
                }}
                size="large"
                style={{ width: "6%" }}
                >
                Cancel
            </SecondaryButton>
        </ButtonContainer>
        )} */}
    </>
    </Descriptions>
    <FormContainer form={form} layout="vertical" initialValues={patient} onFinish={onFinish}>

        <>

            {/* <Descriptions layout="vertical" colon={false}>
                <Descriptions.Item label="Patient ID">{patient.id}</Descriptions.Item>
                <Descriptions.Item label="Patient Name">
                    {edit ? (
                    <Form.Item name="patient_name" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required:
                        true, message: "Name is required" }]}>
                        <Input placeholder="Patient Name" defaultValue="Basma" />
                    </Form.Item>
                    ) : (
                    <>{patient.patient_name}</>
                    )}
                </Descriptions.Item>
                <Descriptions.Item label="Birth Date">
                    {edit ? (
                    <Form.Item name="birth_date" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required:
                        true, message: "Birth Date is required" }]}>
                        <Input placeholder="Birth Date" type="date" onChange={handleBirthDateChange} />
                    </Form.Item>
                    ) : (
                    <>{patient.birth_date}</>
                    )}
                </Descriptions.Item>
                <Descriptions.Item label="Email">
                    {edit ? (
                    <Form.Item name="email" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required: true,
                        message: "Email is required" }]}>
                        <Input />
                    </Form.Item>
                    ) : (
                    <> {patient.email}</>
                    )}
                </Descriptions.Item>
                <Descriptions.Item label="Phone">
                    {edit ? (
                    <Form.Item name="phone_number" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required:
                        true, message: "Phone is required" }]}>
                        <Input />
                    </Form.Item>
                    ) : (
                    <>{patient.phone_number}</>
                    )}
                </Descriptions.Item>
                <Descriptions.Item label="Age">{patient.age}</Descriptions.Item>
                <Descriptions.Item label="Gender">
                    {edit ? (
                    <Form.Item name="gender" labelCol={{ span: 24 }} wrapperCol={{ span: 24 }} rules={[{ required: true,
                        message: "Gender is required" }]}>
                        <Radio.Group buttonStyle="solid">
                            <Radio.Button value="male">Male</Radio.Button>
                            <Radio.Button value="female">Female</Radio.Button>
                        </Radio.Group>
                    </Form.Item>
                    ) : (
                    <>{patient.gender}</>
                    )}
                </Descriptions.Item> */}
                {/* <Descriptions.Item label="Created At"> */}
                    {/* {formatDateTime(patient.created_at)} */}
                    {/* {patient.created_at} */}
                    {/* </Descriptions.Item> */}
                {/*
            </Descriptions>
            {edit && (
            <ButtonContainer>
                <PrimaryButton htmlType="submit" size="large" style={{ width: "6%" }}>
                    Save
                </PrimaryButton>
                <SecondaryButton onClick={()=> {
                    setIsEditing(false);
                    }}
                    size="large"
                    style={{ width: "6%" }}
                    >
                    Cancel
                </SecondaryButton>
            </ButtonContainer>
            )} */}
        </>